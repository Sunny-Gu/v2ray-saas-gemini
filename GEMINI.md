# V2Ray SaaS 平台开发任务规划 (GEMINI.md)

## 1. 项目概述

### 1.1 项目目标
根据《v2ray SaaS 代理服务平台需求规格说明书》，构建一个功能完备、安全可靠、可扩展的微服务化SaaS平台。

### 1.2 技术栈
- **后端语言**: Go
- **Web框架**: Gin / Echo (待定)
- **数据库**: MySQL / PostgreSQL (待定)
- **ORM**: GORM
- **缓存**: Redis
- **配置管理**: Viper
- **服务间通信**: gRPC
- **版本控制**: Git

### 1.3 微服务架构划分
- **门户网站API服务 (`portal-api`)**: 面向普通用户，处理注册、登录、购买、订阅等请求。
- **后台管理API服务 (`admin-api`)**: 面向平台管理员，处理用户管理、节点配置、财务审计等请求。
- **节点服务 (`node-service`)**: 核心服务，负责与代理节点通信、下发用户配置、接收流量统计数据、监控节点状态等。

---

## 2. 初期准备与环境设置 (Phase 1)

- [ ] **1. Git版本控制初始化**
    - [ ] `git init` 初始化本地仓库。
    - [ ] 创建 `.gitignore` 文件，并添加Go项目常见忽略项（如编译产物、`vendor`目录等）。
    - [ ] 创建 `README.md` 项目说明文件。
    - [ ] 建立远程仓库 (GitHub/GitLab) 并关联。

- [ ] **2. 建立项目基础结构**
    - [ ] 创建 `cmd` 目录，用于存放三个微服务的主程序入口 (`main.go`)。
        - [ ] `cmd/portal-api/`
        - [ ] `cmd/admin-api/`
        - [ ] `cmd/node-service/`
    - [ ] 创建 `internal` 目录，用于存放项目内部共享代码。
        - [ ] `internal/models`: 数据库实体模型
        - [ ] `internal/database`: 数据库连接与操作
        - [ ] `internal/redis`: Redis连接与操作
        - [ ] `internal/config`: 配置文件加载
        - [ ] `internal/utils`: 通用工具函数 (如密码处理, JWT)
        - [ ] `internal/middleware`: API中间件
    - [ ] 创建 `configs` 目录，存放服务配置文件 (`config.yaml`)。
    - [ ] 创建 `api` 目录，存放 `.proto` 文件用于gRPC服务定义。

---

## 3. TODO LIST 详细任务

### 3.1 通用/共享模块 (`internal`)

- [ ] **数据库模块**:
    - [ ] 设计并实现所有数据表结构 (参考规格说明书5.1节)。
    - [ ] 使用 GORM 定义 `User`, `Balance`, `Subscription`, `RechargeOrder`, `Node` 等核心模型。
    - [ ] 封装数据库连接、初始化及CRUD基础操作。
- [ ] **配置模块**:
    - [ ] 使用 Viper 实现配置文件的加载与解析。
- [ ] **日志模块**:
    - [ ] 集成一个结构化日志库 (如 `logrus` 或 `zap`)。
- [ ] **工具模块**:
    - [ ] 实现密码加密与验证功能 (bcrypt)。
    - [ ] 实现 JWT (JSON Web Token) 的生成与解析功能。
- [ ] **API响应封装**:
    - [ ] 定义统一的JSON响应结构体。

### 3.2 门户网站API服务 (`portal-api`) (Phase 2)

- [ ] **用户账户管理**:
    - [ ] 实现用户注册接口 (含邮箱验证)。
    - [ ] 实现用户登录接口 (返回JWT)。
    - [ ] 实现密码找回接口。
- [ ] **个人中心**:
    - [ ] 获取用户基本信息接口。
    - [ ] 获取用户余额、套餐状态、流量使用情况接口。
    - [ ] 查询消费历史记录接口。
- [ ] **充值系统**:
    - [ ] 实现创建USDT充值订单接口。
    - [ ] 实现充值码兑换接口。
- [ ] **充值码系统**:
    - [ ] 用户生成充值码接口 (扣除余额)。
    - [ ] 查看已生成充值码列表接口。
- [ ] **订阅管理**:
    - [ ] 获取/重置用户订阅链接接口。
- [ ] **商店系统**:
    - [ ] 获取所有可售套餐列表接口。
    - [ ] 用户购买/升级套餐接口 (余额支付)。
- [ ] **节点状态**:
    - [ ] 获取所有节点实时状态列表接口。
- [ ] **工单系统**:
    - [ ] 用户提交/查看/回复工单接口。
- [ ] **内容系统**:
    - [ ] 获取帮助中心文档接口。
    - [ ] 获取平台公告接口。

### 3.3 后台管理API服务 (`admin-api`) (Phase 3)

- [ ] **管理员认证**:
    - [ ] 实现管理员登录接口 (含2FA)。
    - [ ] 实现JWT认证中间件，保护后台接口。
- [ ] **账户管理**:
    - [ ] 实现用户列表查询接口 (支持搜索、分页)。
    - [ ] 实现用户账户操作接口 (锁定/解锁, 余额调整)。
- [ ] **节点管理**:
    - [ ] 实现节点的增、删、改、查 (CRUD) 接口。
- [ ] **订单系统**:
    - [ ] 实现充值订单与订阅订单的查询与管理接口。
- [ ] **充值码管理**:
    - [ ] 管理员批量生成充值码接口。
    - [ ] 全站充值码查询与状态管理接口。
- [ ] **配置管理**:
    - [ ] 实现USDT充值相关配置接口 (汇率, 折扣)。
    - [ ] 实现商店套餐的增、删、改、查 (CRUD) 接口。
- [ ] **工单处理**:
    - [ ] 实现工单分配、处理、回复接口。
- [ ] **内容管理**:
    - [ ] 实现帮助文档和公告的增、删、改、查 (CRUD) 接口。
- [ ] **资金审计**:
    - [ ] 实现资金总览数据统计接口。
    - [ ] 实现用户资金审计与全站资金平衡检查接口。

### 3.4 节点服务 (`node-service`) (Phase 4)

- [ ] **与v2ray节点通信**:
    - [ ] 提供API接口，供v2ray后端查询有效用户列表及配置。
    - [ ] 提供API接口，供v2ray后端上报用户流量使用数据。
- [ ] **流量统计与处理**:
    - [ ] 接收节点上报的流量数据，进行聚合处理并写入数据库。
    - [ ] 实现流量超限自动处理逻辑。
- [ ] **节点状态监控**:
    - [ ] 实现定时任务，周期性检测所有节点的在线状态和延迟。
    - [ ] 将节点状态缓存至Redis，供其他服务快速查询。
- [ ] **服务间gRPC接口**:
    - [ ] 定义并实现gRPC接口，供 `portal-api` 和 `admin-api` 查询节点状态、用户信息等。

---

## 4. 开发流程

本项目严格遵循 **代码编写 -> 测试 -> 提交** 的流程。

1.  **分支管理**:
    - `main`: 主分支，保持稳定，用于生产部署。
    - `develop`: 开发分支，集成所有已完成的功能。
    - `feature/<feature-name>`: 功能开发分支，每个新功能或修复都应在此类分支上进行。例如: `feature/user-login`。

2.  **编码 (Code)**:
    - 从 `develop` 分支创建新的 `feature` 分支。
    - 在功能分支上进行代码编写，遵循Go语言编码规范。

3.  **测试 (Test)**:
    - 为核心业务逻辑编写单元测试。
    - 使用 `go test ./...` 运行所有测试，确保新增代码不会破坏现有功能。
    - 在 `develop` 环境进行接口功能测试。

4.  **提交 (Commit)**:
    - 使用有意义的提交信息，推荐采用 `Conventional Commits` 规范。
        - `feat`: 新功能
        - `fix`: Bug修复
        - `docs`: 文档变更
        - `style`: 代码格式调整
        - `refactor`: 代码重构
        - `test`: 新增或修改测试
    - **示例**: `git commit -m "feat(portal): implement user registration endpoint"`
    - 将 `feature` 分支推送到远程仓库，并发起 `Pull Request` (或 `Merge Request`) 到 `develop` 分支。

5.  **代码审查 (Code Review)**:
    - 团队成员对 `Pull Request` 进行审查，通过后合并到 `develop` 分支。

---

## 5. 下一步行动

- [ ] **执行 `Phase 1`**: 从“初期准备与环境设置”开始，搭建好项目骨架。
- [ ] **逐步完成 `Phase 2` 到 `Phase 4`** 的各项开发任务。
